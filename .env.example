# Copy to .env and adjust. Every value is optional.

# Host port the site is published on (container always listens on 3000).
HOST_PORT=3000

# Timezone used for dates rendered by the container.
TZ=America/New_York
